---
phase: 01-publisher-import-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/src/server.ts
  - backend/src/auth/auth.ts
  - backend/src/auth/fastifyAuthRoute.ts
  - backend/src/db/db.ts
  - backend/src/db/schema/auth.ts
  - backend/src/routes/health.ts
  - backend/src/routes/session.ts
  - frontend/vite.config.ts
  - frontend/package.json
  - frontend/src/App.tsx
  - frontend/src/pages/SignIn.tsx
  - frontend/src/components/Navbar.tsx
  - frontend/src/lib/authClient.ts
autonomous: true
user_setup:
  - service: github_oauth_app
    why: "GitHub OAuth sign-in for publishers"
    env_vars:
      - name: GITHUB_CLIENT_ID
        source: "GitHub -> Settings -> Developer settings -> OAuth Apps"
      - name: GITHUB_CLIENT_SECRET
        source: "GitHub -> Settings -> Developer settings -> OAuth Apps"
      - name: BETTER_AUTH_SECRET
        source: "Generate a random 32+ byte secret for signing"
      - name: DATABASE_URL
        source: "SQLite file path (e.g. file:./dev.db)"
    dashboard_config:
      - task: "Create OAuth App"
        location: "GitHub -> Settings -> Developer settings -> OAuth Apps"
      - task: "Set Authorization callback URL to backend auth callback route"
        location: "OAuth App settings -> Authorization callback URL"

must_haves:
  truths:
    - "Publisher can start GitHub OAuth sign-in from /signin"
    - "Publisher session can be checked via an API endpoint"
    - "Publisher session state persists across a browser refresh (cookie-based session)"
  artifacts:
    - path: "backend/src/auth/auth.ts"
      provides: "Better Auth configuration with GitHub provider"
    - path: "backend/src/server.ts"
      provides: "Fastify server exposing /api/auth/* and API routes"
    - path: "frontend/src/App.tsx"
      provides: "App-level auth bootstrap (session check on load)"
    - path: "frontend/vite.config.ts"
      provides: "Dev proxy for /api to backend"
  key_links:
    - from: "frontend/src/pages/SignIn.tsx"
      to: "/api/auth/sign-in/social"
      via: "better-auth client signIn.social"
      pattern: "signIn\\.social"
    - from: "frontend/src/App.tsx"
      to: "/api/auth/session"
      via: "fetch with credentials"
      pattern: "fetch\(.*\/api\/auth\/session"

---

<objective>
Implement a real GitHub OAuth sign-in flow with a durable cookie session, and wire the existing SPA to reflect auth state on load.

Purpose: Satisfies AUTH-01/AUTH-02 and unblocks authenticated publisher endpoints in later plans.
Output: Running backend with /api/auth/* + session-check route, and frontend that redirects to OAuth + persists logged-in state across refresh.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-publisher-import-pipeline/01-RESEARCH.md

@frontend/src/App.tsx
@frontend/src/pages/SignIn.tsx
@frontend/src/components/Navbar.tsx
@frontend/vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend scaffold + Better Auth GitHub OAuth</name>
  <files>backend/package.json, backend/src/server.ts, backend/src/auth/auth.ts, backend/src/auth/fastifyAuthRoute.ts, backend/src/db/db.ts, backend/src/db/schema/auth.ts, backend/src/routes/health.ts, backend/src/routes/session.ts</files>
  <action>
Create a new `backend/` Node.js (ESM) + TypeScript service using Fastify.

Backend runtime defaults (make these true unless already established differently in repo):
- Listen on `PORT=8787`.
- Use `DATABASE_URL=file:./dev.db` for local development.

`backend/package.json` must include, at minimum, these scripts so later plans can rely on them:
- `dev`: start the Fastify server in watch mode
- `build`: typecheck + build to `backend/dist/`
- `lint`: run eslint
- `db:generate`: `drizzle-kit generate`
- `db:migrate`: `drizzle-kit migrate`

Implementation requirements:
- Use `better-auth` for GitHub OAuth and cookie-based sessions; do not hand-roll OAuth.
- Use Drizzle + SQLite for persistence (session must survive process restart).
- Expose Better Auth handler under `GET|POST /api/auth/*`.
- Add a small `GET /api/auth/session` route that returns `{ user: { id, name, email }, signedIn: boolean }` based on the request cookie.

Integration requirements:
- Configure CORS only if needed; prefer Vite dev proxy so the SPA can call `/api/*` without cross-origin config.
- Ensure cookie settings are compatible with local dev (localhost) and typical deployments (HttpOnly, SameSite=Lax; Secure only in production).

Notes for correctness:
- Use the `better-auth_setup_auth` tool to generate the canonical Better Auth + Fastify + Drizzle + SQLite integration files, then adapt paths to the `backend/src/...` structure.
- Add a simple `/api/health` route returning 200 JSON for quick sanity checks.
- Configure GitHub provider redirect URI explicitly (expected pattern: `http://localhost:8787/api/auth/callback/github` in dev).
  </action>
  <verify>
In one terminal: `npm --prefix backend install && npm --prefix backend run dev`

In another terminal:
- `curl -sS http://localhost:8787/api/health`
- `curl -i -X POST http://localhost:8787/api/auth/sign-in/social -H 'content-type: application/json' -d '{"provider":"github","disableRedirect":true}'` (expect 200/302 initiating OAuth)
- `curl -sS http://localhost:8787/api/auth/session` (expect `signedIn:false` when not logged in)
  </verify>
  <done>
- Backend starts and serves `/api/auth/*`, `/api/auth/session`, and `/api/health`.
- `/api/auth/signin/github` returns a redirect response.
- `/api/auth/session` returns a stable JSON shape suitable for the SPA.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire SPA auth bootstrap and GitHub sign-in redirect</name>
  <files>frontend/vite.config.ts, frontend/package.json, frontend/src/lib/authClient.ts, frontend/src/App.tsx, frontend/src/pages/SignIn.tsx, frontend/src/components/Navbar.tsx</files>
  <action>
Replace the in-memory `isLoggedIn` simulation with a real session check.

Implementation requirements:
- Add a Vite dev server proxy for `/api` to the backend origin (default `http://localhost:8787`).
- In `App.tsx`, fetch `/api/auth/session` on initial mount (with `credentials: 'include'`) and derive `isLoggedIn` from the response.
- Add `better-auth` client usage in the SPA (create `frontend/src/lib/authClient.ts`) and update the Sign In button to call `authClient.signIn.social({ provider: 'github', callbackURL: '/dashboard' })`.
- Update `Navbar.tsx` Sign In button to navigate to `/signin` (not to fake-login + /dashboard).

Non-goals (explicit):
- Do not implement webhooks or auto-sync.
- Do not build any repo registration or import UI wiring yet.
  </action>
  <verify>
`npm --prefix frontend run dev`

Then:
- Load `http://localhost:5173/signin` and click the GitHub sign-in button (should navigate away to GitHub).
- Without completing OAuth, refresh page: app remains in signed-out state (no crashes).
  </verify>
  <done>
- SPA calls `/api/auth/session` on load and does not rely on in-memory login state.
- Sign-in UI triggers OAuth redirect via backend.
  </done>
</task>

</tasks>

<verification>
- `npm --prefix backend run build` succeeds.
- `npm --prefix frontend run build` succeeds.
</verification>

<success_criteria>
- AUTH-01 implemented: OAuth redirect route exists and is wired from the SPA.
- AUTH-02 implemented: session persistence is cookie + DB backed (no local-only auth state).
</success_criteria>

<output>
After completion, create `.planning/phases/01-publisher-import-pipeline/01-01-SUMMARY.md`
</output>
