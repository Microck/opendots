---
phase: 02-great-bundle-pages-safety-downloads
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - backend/src/routes/publicBundles.ts
  - backend/src/import/zipGenerator.ts
  - frontend/src/pages/Detail.tsx
  - frontend/src/pages/Detail.module.css
  - frontend/src/pages/Docs.tsx
  - frontend/src/pages/Docs.module.css
autonomous: true

must_haves:
  truths:
    - "Visitor can see validation results and risk flags on the bundle detail page"
    - "Visitor sees a clear 'user responsible' safety disclaimer"
    - "Visitor can download a Project ZIP variant"
    - "Visitor can download a Global ZIP variant"
    - "Visitor can read install instructions for each download type"
    - "Visitor can read a docs page explaining bundles, install destinations, and safety"
  artifacts:
    - path: "backend/src/import/zipGenerator.ts"
      provides: "ZIP generation for project and global layout variants"
    - path: "frontend/src/pages/Detail.tsx"
      provides: "Safety display + download cards + install instructions"
    - path: "frontend/src/pages/Docs.tsx"
      provides: "Docs page with real bundle/install/safety content"
  key_links:
    - from: "frontend/src/pages/Detail.tsx"
      to: "/api/bundles/:id/download?variant=project"
      via: "download link"
      pattern: "\/download\?variant="
    - from: "frontend/src/pages/Detail.tsx"
      to: "safetyResults from bundle API"
      via: "renders validation + risk flags"
      pattern: "safetyResults|riskFlags|validation"
---

<objective>
Add safety signal display, download functionality (Project + Global ZIP variants), install instructions, and wire the Docs page with real content.

Purpose: Completes the "great bundle page" experience — SAFE-06 (safety messaging), DL-01/DL-02/DL-03 (downloads + install instructions), and DOCS-01 (documentation page). This is the final Phase 2 plan that makes bundles fully evaluable and installable.
Output: Download endpoints streaming layout-specific ZIPs, safety UI on detail page, and comprehensive docs page.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-great-bundle-pages-safety-downloads/02-RESEARCH.md

@frontend/src/pages/Detail.tsx
@frontend/src/pages/Docs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ZIP download endpoints for Project and Global layouts</name>
  <files>backend/src/import/zipGenerator.ts, backend/src/routes/publicBundles.ts</files>
  <action>
Create `backend/src/import/zipGenerator.ts` that generates download ZIPs from a snapshot:

**Project ZIP layout (DL-01):**
- Root `opencode.json*` stays at root
- `.opencode/` directory tree preserved as-is
- Result: user extracts into project root and gets a working project-scoped config

**Global ZIP layout (DL-02):**
- Remap `.opencode/` contents to top-level (so extracting into `~/.config/opencode/` works)
- Include `opencode.json*` at root (user merges manually)
- Result: user extracts into `~/.config/opencode/` for global config

Implementation:
- Use `archiver` to stream ZIPs on demand from the snapshot zip contents.
- Normalize all paths: strip leading `/`, reject `..` segments (zip-slip prevention).
- Set `Content-Disposition: attachment; filename="<bundle-slug>-project.zip"` (or `-global.zip`).

Add endpoint to public routes:
- `GET /api/bundles/:id/download?variant=project|global` → streams the appropriate ZIP.
- Return 400 if variant is missing or invalid.
- Return 404 if bundle or snapshot not found.

Install backend dep: `npm install --prefix backend archiver && npm install --prefix backend -D @types/archiver`
  </action>
  <verify>
`npm --prefix backend run lint && npm --prefix backend run build`

With backend running and a bundle imported:
- `curl -sS -o /tmp/test-project.zip "http://localhost:8787/api/bundles/<BUNDLE_ID>/download?variant=project"` → should produce a valid zip
- `curl -sS -o /tmp/test-global.zip "http://localhost:8787/api/bundles/<BUNDLE_ID>/download?variant=global"` → should produce a valid zip
- `unzip -l /tmp/test-project.zip` → should show project layout paths
- `unzip -l /tmp/test-global.zip` → should show global layout paths
  </verify>
  <done>
- Both ZIP variants are streamable and produce correct file layouts.
- Path traversal is prevented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add safety display, download cards, install instructions to Detail page</name>
  <files>frontend/src/pages/Detail.tsx, frontend/src/pages/Detail.module.css</files>
  <action>
Extend the Detail page (wired to real data from Plan 02-01) with three new sections:

**Safety section (SAFE-06):**
- Display validation summary: config valid/invalid, theme valid/invalid, skill valid/invalid — with error details expandable.
- Display risk flag badges prominently: `EXEC`, `SHELL`, `REMOTE`, `EVAL` — using the site's red risk badge style (`#ff6b6b`).
- Display secret scan warnings if any (count + expandable list).
- Include a clear disclaimer block: "Opendots performs best-effort scanning. Bundles may contain executable code. **You are responsible for reviewing what you install.**" Use a distinct visual treatment (bordered box, warning icon).

**Download section (DL-01, DL-02, DL-03):**
- Two download cards side by side: "Project ZIP" and "Global ZIP".
- Each card has: layout description, download button linking to `/api/bundles/:id/download?variant=project|global`, and brief install instructions.
- Project install: "Extract to your project root. Config applies to this project only."
- Global install: "Extract to `~/.config/opencode/`. Config applies to all projects."

**Non-goals:**
- Do not change the file tree / CodeExplorer section (already done in 02-01).
- Do not add community features (ratings, comments).

Style everything with CSS Modules consistent with the existing design system (dark blue background, sharp edges, monospace accents).
  </action>
  <verify>
`npm --prefix frontend run build`

Visit `http://localhost:5173/bundle/<BUNDLE_ID>`:
- Safety section shows validation results and any risk badges.
- Disclaimer text is visible and prominent.
- Two download cards are visible with working download links.
  </verify>
  <done>
- Safety signals, risk badges, and disclaimer are prominently displayed.
- Download buttons work for both Project and Global ZIP variants.
- Install instructions are clear and visible.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire Docs page with real bundle/install/safety content</name>
  <files>frontend/src/pages/Docs.tsx, frontend/src/pages/Docs.module.css</files>
  <action>
Replace the static placeholder content in Docs.tsx with comprehensive documentation (DOCS-01):

Sections to include:
1. **What is a bundle?** — Explain that bundles are collections of OpenCode configuration artifacts (themes, commands, agents, modes, skills, plugins, tools, rules, config). Explain the `opendots-<slug>` repo format and `opendots.yml` manifest.

2. **Install destinations:**
   - **Project scope:** Extract to project root → `opencode.json` + `.opencode/` → applies only in that directory.
   - **Global scope:** Extract to `~/.config/opencode/` → applies to all projects.
   - Explain `OPENCODE_CONFIG_DIR` as an override (brief mention).

3. **Safety model:**
   - Explain that bundles can contain executable code (plugins, tools, shell commands).
   - Explain that Opendots performs best-effort scanning (schema validation, risk flag detection, secret scanning) but does NOT guarantee safety.
   - "You are the final firewall. Always review bundle contents before installing."
   - Explain what risk badges mean (EXEC, SHELL, REMOTE, EVAL).

4. **How to publish:**
   - Sign in with GitHub → create repo `opendots-<slug>` → add `opendots.yml` → register on Opendots → system validates + imports.

Keep the existing CSS Module styling approach. Content should be static (no API calls needed).
  </action>
  <verify>
`npm --prefix frontend run build`

Visit `http://localhost:5173/docs`:
- All four sections are readable and styled consistently.
- Install instructions mention both project and global scope.
- Safety section clearly states "user responsible" model.
  </verify>
  <done>
- Docs page explains bundles, install destinations, safety model, and publishing.
- Content is accurate and matches the actual system behavior.
  </done>
</task>

</tasks>

<verification>
- `npm --prefix backend run build` succeeds.
- `npm --prefix frontend run build` succeeds.
- Bundle detail page shows safety + downloads + instructions.
- Docs page has comprehensive real content.
</verification>

<success_criteria>
- SAFE-06: Safety model clearly communicated with disclaimer.
- DL-01: Project ZIP downloadable.
- DL-02: Global ZIP downloadable.
- DL-03: Install instructions provided for each variant.
- DOCS-01: Docs page explains bundles, install, and safety.
</success_criteria>

<output>
After completion, create `.planning/phases/02-great-bundle-pages-safety-downloads/02-03-SUMMARY.md`
</output>
